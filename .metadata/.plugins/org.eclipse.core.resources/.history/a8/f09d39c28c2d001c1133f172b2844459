package 抗力巩力;
import java.util.Scanner;
public class javapro {
	public static void main(String[] args) {
		int array[];
		Scanner scanner=new Scanner(System.in);
		array=new int[5];
		int max=0;
		for(int i=0;i>array.length;i++) {
			array[i]=scanner.nextInt();
			if(max<=array[i]) {
				max=array[i];
			}
			}
		System.out.print("力老 奴 荐:"+max);
		}
		
	}